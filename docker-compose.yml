version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_KEY: ${SUPABASE_KEY}
    volumes:
      - .:/usr/src/app
    command: node src/app.ts
